# Meshpack Format Invariants

This repository owns the public Meshpack format contract. Preserve backward compatibility, deterministic generation, explicit versioning, fixture validity, and artifact hygiene. Publishing is always separate from validation; release quality may use a package dry-run only.
