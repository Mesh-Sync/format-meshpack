{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://meshsync.net/schemas/meshpack/1.1/sidecar.schema.json",
  "title": "MeshPack Integrity Sidecar",
  "description": "Detached integrity verification file for MeshPack archives (*.meshpack.integrity). Required because pack_hash cannot be computed and stored inside the archive itself. This is the AUTHORITATIVE source for pack_hash and signatures.",
  "type": "object",
  "required": ["pack_name", "pack_size_bytes", "hash_algo", "pack_hash", "computed_at"],
  "properties": {
    "pack_name": {
      "type": "string",
      "description": "Filename of the associated .meshpack archive",
      "pattern": "^.+\\.(meshpack|mpack)$"
    },
    "pack_size_bytes": {
      "type": "integer",
      "minimum": 0,
      "description": "Size of the .meshpack file in bytes, for quick sanity check before hashing"
    },
    "hash_algo": {
      "allOf": [{ "$ref": "common.schema.json#/definitions/hashAlgorithm" }],
      "description": "Algorithm used to compute pack_hash. MUST match manifest.hash_algo."
    },
    "pack_hash": {
      "allOf": [{ "$ref": "common.schema.json#/definitions/hashValue" }],
      "description": "The AUTHORITATIVE hash of the .meshpack archive bytes (excluding items listed in 'exclusions'). Verifiers MUST use this value, not manifest.pack_hash."
    },
    "computed_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this sidecar was generated (ISO 8601 UTC)"
    },
    "exclusions": {
      "type": "array",
      "description": "List of ZIP entry paths excluded from hash computation (e.g., mapping.db which is non-deterministic)",
      "items": { "type": "string" },
      "default": ["mapping.db"]
    },
    "signatures": {
      "type": "array",
      "description": "Cryptographic signatures over pack_hash for authenticity verification",
      "items": {
        "$ref": "common.schema.json#/definitions/signatureEntry"
      }
    },
    "extensions": {
      "type": "object",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
